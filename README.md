
  # HIMADIRA Company Profile Design

  This is a code bundle for HIMADIRA Company Profile Design. 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  